# ГИГА-ПРОМПТ — главная страница: довести до monkeytype полностью

> Скопируй всё, что ниже разделителя, в сессию Claude Code, открытую
> в папке `C:\Users\Muhammadali\Desktop\Speedtype2`.

---

## РОЛЬ

Ты доводишь **главную страницу** Speedtype 2 до полного совпадения с
monkeytype. Только её — но целиком: шапку, футер, панель режимов, строку
активных режимов, область слов, каретку, живую статистику, служебные
элементы, горячие клавиши и экран результата.

Это половина всего проекта: в аудите по главной **270 пунктов из 552**.

---

## ПЕРВОЕ И САМОЕ ВАЖНОЕ: отчёт устарел

`audit/report.md` снят до того, как по проекту прошлись правки. С тех пор
`pages/test.ts` вырос с 827 строк примерно до двух тысяч и продолжает расти
на глазах — за один час он был 1694, 1897, 1951 и 1953. **Не верь никакому
числу строк, включая это: меряй сам в момент работы.**

```bash
wc -l frontend/src/pages/test.ts
```

**Разделы «Шапка» и «Футер» ниже закрыты почти целиком** — перепроверено
по коду 20 августа. Не чини повторно:

- индикатор темы и версия в футере (36, 37), ссылка github (33), кнопка
  «наверх» (38), гашение футера и подсказок при наборе (39, 30);
- подсказки клавиш переехали в оболочку над футером — `#testHint`
  в `main.ts`, а не под тестом (29);
- шапка гаснет при наборе: `header[data-focused]` ставится из `test.ts` (9),
  подпись логотипа гаснет вместе с ней (8);
- пункт «о проекте» и страница `/about` (13);
- `data-nav-item="leaderboards"` (12) и `data-ui-element` на `header`,
  `logo`, `logoText`, `logoSubtext`, `testConfig`;
- подсветка активного пункта: роутер целится в `[data-nav-item]`,
  а не в несуществующий `.nav-link` (26);
- кнопки навигации 2.25rem = 36px с паддингом и скруглением, иконка 1rem,
  зазор в шапке 8px (23, 24, 10);
- `aria-label` и `data-balloon-pos` на всех пунктах навигации (27).

Также закрыто вне этих разделов: `esc` как рестарт (218) — строки
`"escape"` в коде больше нет; режимы `zen` и `custom` (59, 60) — в схеме
уже пять значений; тема применяется через `<style>`, а не inline;
заглушек в настройках 4 вместо 24, нерабочих funbox 2 вместо 10;
контейнер `#testModesNotice` для строки активных режимов уже размечен —
проверь, сколько индикаторов в нём реально живёт, прежде чем делать
раздел 4 с нуля.

**Актуальным остаётся главное визуальное**: огромный таймер за словами
(174), полоса таймера на весь экран (173), три карточки панели (44),
наполнение строки активных режимов (76–94), числа каретки и размер шрифта
слов.

**Не реализуй ничего по отчёту вслепую.** Порядок такой:

1. Возьми раздел отчёта.
2. Открой текущий код и проверь **фактическое** состояние каждого пункта.
3. Отметь, что уже сделано, что осталось, что сделано наполовину.
4. Только потом чини.

Если увидишь, что пункт закрыт — так и запиши, это тоже результат. Отчёт
о том, что «сделал» уже сделанное другим, хуже, чем отсутствие отчёта.

Проверь заодно, не правит ли кто-то файлы прямо сейчас:

```bash
find frontend/src -name "*.ts" | xargs ls -lt | head -5
```

Если время правок — последние минуты, значит в файлах работает другая
сессия. Тогда остановись и скажи мне: одновременная запись в `test.ts`
затрёт чужую работу.

---

## ЧТО СЧИТАЕТСЯ ГЛАВНОЙ СТРАНИЦЕЙ

Десять разделов отчёта, пункты 1–270:

| раздел | что | пункты | файл |
|---|---|---|---|
| 1 | шапка и навигация | 1–28 | `main.ts:19-66` |
| 2 | футер | 29–42 | `main.ts:29-36` |
| 3 | панель режимов | 43–75 | `test.ts:168-259` |
| 4 | строка активных режимов | 76–94 | **блока нет вообще** |
| 5 | область слов | 95–145 | `test.ts:263-340` |
| 6 | каретка | 146–171 | `test.ts:342-379` |
| 7 | живая статистика | 172–187 | `test.ts:394-449` |
| 8 | служебные элементы | 188–215 | `test.ts` |
| 9 | горячие клавиши и ввод | 216–233 | `test.ts:680-736` |
| 10 | экран результата | 234–270 | `test.ts:572-667` |

Плюс **раздел 18** — 40 точных замеров стилей, снятых `getComputedStyle`
с обоих сайтов на теме `serika_dark` в окне 1440×900. Это твои эталонные
числа, мерить заново не надо.

Клон monkeytype — в `%TEMP%\mtref\mt`. Если нет, склонируй мелкой копией.

---

## ГЛАВНОЕ ВИЗУАЛЬНОЕ ОТЛИЧИЕ — начни с него

В отчёте это пункт 174, и там прямо сказано: **это главное визуальное
отличие главной страницы.**

У monkeytype таймер в режиме `text` — **огромный, от 4rem до 10rem по
брейкпоинтам, отрисован абсолютно над словами с `z-index: -1`**, то есть
буквально за текстом. Именно это делает их главную узнаваемой.

У нас — 2rem, обычным потоком, отдельной строкой над словами.

Рядом два родственных пункта:

- **173** — таймер `bar` у них `position: fixed` во всю ширину окна,
  высота 8px, цвет по настройке. У нас полоса 6.4px внутри блока
  статистики, всегда цветом `main`.
- **175** — таймер `mini` у нас **не существует как ветка**: значение
  выбирается, а ведёт себя как `text`. Помечен рабочим, хотя не работает.

Оговорка при переносе: если таймер уходит за текст через `z-index: -1`,
у `#words` не должно быть собственного фона — иначе таймер пропадёт.

---

## РАЗДЕЛ ЗА РАЗДЕЛОМ — что чинить

Полные таблицы в отчёте. Здесь то, что решает вид и поведение.

### Шапка (1–28) — почти закрыта

Сделано: 8, 9, 10, 12, 13, 23, 24, 26, 27 и атрибуты `data-ui-element`.
Список выше, в блоке про устаревание. Осталось три вещи:

- **6** — типографика логотипа. Атрибут `data-ui-element="logoText"` уже
  стоит, но начертание не приведено: у нас `--font-weight-bold` и
  `--tracking-tight`, у monkeytype **вес 400 и letter-spacing normal**.
  Правится в токенах, а не по месту.
- **3** — клик по логотипу на странице теста должен перезапускать тест.
  Сейчас просто переход на `/`.
- **15–18** — уведомления в навигации, аватар и уровень у пункта аккаунта,
  выпадающее меню аккаунта, полоса опыта. Все четыре требуют серверной
  части (xp, уведомления) — это не работа по главной, отложи.

### Футер (29–42) — закрыт

Индикатор темы, версия, github, кнопка «наверх», гашение при наборе,
переезд подсказок клавиш — всё сделано и проверено по коду.

Остались только пункты, по которым в отчёте стоит «не нужно»: ссылки
support, discord, twitter — это их сообщество, а не наше. Ссылку
«contact» (31) и юридические страницы (35) сделай, если решишь
публиковать проект; страница `/about` с текстом GPL уже закрывает
минимальное обязательство.

### Панель режимов (43–75)

- **44** — **три отдельные карточки** вместо одной с пустыми разделителями.
- **49** — отступ кнопок `12px 6px`; у нас `3.2px 8.8px`, из-за чего вся
  панель вдвое тоньше.
- **47, 48, 50, 51** — паддинг карточки, шрифт 0.75rem, зазор 6px,
  адаптивный зазор между карточками.
- **65** — в режиме цитаты группа длин **скрыта целиком**. Показать.
- **62, 64** — модалки своего времени и своего числа слов.
- **71** — анимация смены группы значений, 250 мс.
- **43** — атрибут `data-ui-element="testConfig"`, его ждут 2 темы.
- **74, 75** — язык и funbox уезжают из панели в строку активных режимов.
- **73** — ниже 849px панель заменяется кнопкой, открывающей модалку.

### Строка активных режимов (76–94)

**Блока нет вообще.** Это один div под панелью, по центру, цветом `sub`,
гаснет при наборе. В нём до 19 индикаторов: язык, funbox, сложность,
слепой режим, pace caret, личный рекорд, ограничения скорости и точности,
confidence, stop on error, delete on error, раскладка, теги, повтор,
выключенное сохранение.

Делай сразу как список правил «условие → индикатор», а не как цепочку
`if` в разметке — иначе через десять индикаторов это станет нечитаемым.

### Область слов (95–145)

- **99** — три строки должен ограничивать **`#wordsWrapper`**, а не
  `#words`. У них `#words` — `height: fit-content`. В wrapper целятся
  темы и funbox.
- **100** — `#words` у нас `display: flex`, из-за чего буквы
  блокифицируются. У них обычный блок с `inline-block` буквами.
- **104** — размер шрифта по умолчанию **32px**, у нас 24px.
- **103, 102** — межстрочный `1em` на слове, поля слова `0.25em 0.3em`.
- **116** — слово с ошибкой: кроме нижней границы у них **обводка
  `text-shadow` цветом фона**. Характерная деталь, у нас её нет.
- **119** — класс слепого режима `#words.blind`, у нас `blindMode`.
  Ждут 4 селектора тем.
- **109** — лишняя буква получает **оба** класса: `incorrect extra`.
- **110, 111** — след исправленной ошибки: пунктир снизу и справа.
- **128, 129, 130** — подсветка: дефисы вместо подчёркиваний, плюс
  значения на две и три слова вперёд. **6 селекторов тем ждут вариант
  на три слова.**
- **125** — `highlight-off` у нас красит все слова, включая активное.
  Ограничить набранными.
- **127** — подсветка словом у них цветом, у нас прозрачностью.
- **131** — классы эффекта ввода `typed-effect-*`, у нас `typed-*`.
- **133** — лента: класс нужен **и** на `#words`, **и** на wrapper.
- **140** — при потере фокуса кроме размытия ещё `opacity: 0.25`.
- **141** — сервер отдаёт флаг RTL, клиент его выбрасывает.

### Каретка (146–171)

- **158** — **мигание не останавливается при наборе.** Заметно глазу,
  чинится примерно в пять строк.
- **148** — стиль по умолчанию должен называться `#caret.default`, у нас
  `caret-line`. Ждут 3 темы. **Осторожно: старое значение уже лежит у
  пользователей в браузере — нужна миграция, иначе каретка исчезнет.**
- **159, 160, 161** — длительности движения **150 / 100 / 85 мс**;
  у нас 250 / 120 / 60.
- **156** — скругление через `--roundness` (8px), у нас жёсткие 1.6px.
- **157** — вторая анимация мигания для `smoothCaret: off`.
- **149** — стиль `block` у них под текстом (`z-index: -1`), у нас поверх
  и полупрозрачный.
- **147** — стиль `off` у них `width: 0`, каретка остаётся в потоке.
- **165, 166, 167** — pace caret в режимах «средняя», «рекорд»,
  «последний» берёт значение ручной настройки. Помечен рабочим — врёт.

### Живая статистика (172–187)

Кроме таймера, о котором выше:

- **180, 181** — живая скорость: у них крупная под словами, `mini` —
  сверху рядом с таймером.
- **178** — прозрачность применяется ко всем живым показателям, не только
  к таймеру.
- **187** — до старта живая статистика не показывается.
- **183** — подпись у burst — единица скорости, а не слово «слово».

### Служебные элементы (188–215)

- **195** — плашка Caps Lock: у них фон `main`, текст цветом фона,
  абсолютно над словами. У нас цвета инвертированы и она в потоке.
- **196** — Caps Lock проверяется только при нажатии внутри поля: если
  Caps включили до начала ввода, предупреждения не будет.
- **188, 189** — подсказка клавиш: две пары вместо трёх, и она **врёт**
  при `quickRestart: esc` и при `off`.
- **213** — кнопка рестарта: паддинг `16px 32px`, у нас вертикаль вдвое
  меньше.
- **215** — скрытое поле ввода должно быть `<textarea>`, а не `<input>`,
  иначе многострочный текст невозможен.
- **206** — контейнер клавиатуры: атрибут `data-ui-element="keymap"`,
  в него целится funbox `crt`.
- **202, 203, 204** — форма `alice`, подписи `dynamic` и ряд `full` —
  объявлены, но правил нет.
- **211, 212** — экран ошибки с кнопкой повтора, спиннер вместо «…».

### Горячие клавиши (216–233)

- **218** — **`esc` как рестарт не работает**: сравнивается `"escape"`
  с `"esc"`. Однострочная правка.
- **220** — **рестарт с клавиатуры мёртв на экране результата**:
  обработчик умирает вместе с `#wordsInput`. Вешать на документ, но с
  проверкой, что фокус не в поле ввода, — иначе рестарт будет срабатывать
  при наборе в модалках.
- **221** — `esc` открывает командную строку, а не ведёт в настройки.
  Это ломает привычку, но это паритет.
- **229** — вставка из буфера молча ничего не делает. Показывать
  уведомление.

### Экран результата (234–270)

- **240, 241** — блоки «тип теста» и «прочее»: сейчас по результату вообще
  не понять, что за тест был. Дёшево и заметно.
- **244** — четыре счётчика символов вместо двух. **Требует движка и
  трогает метрики** — см. «Опасность».
- **251, 252, 253, 256** — график: raw пунктиром, burst, крестики ошибок,
  легенда из шести переключателей, подписи оси X.
- **249** — источник цитаты: **сервер его уже отдаёт, клиент выбрасывает.**
- **259–264** — шесть кнопок: следующий тест, тот же текст, практика
  ошибочных слов, история слов, реплей, скриншот.
- **265** — при обрыве сети кнопка «повторить сохранение».
- **238, 239** — корона рекорда со старым значением и анимацией.
- **237** — крупное число ~4rem, у нас 3rem.

**Отдельно, вне отчёта:** экран результата рисуется только после ответа
сервера, а таймаута у запросов нет. Если запрос зависнет, результат не
покажется никогда, и тест останется в подвешенном состоянии. Показывай
цифры сразу, сохраняй фоном, добавь запросам таймаут.

---

## ЭТАЛОННЫЕ ЧИСЛА

Из раздела 18 отчёта, применимые к главной:

| | monkeytype | у нас |
|---|---|---|
| ширина контента | 1024 + breakout | 1000px |
| боковые поля | 2rem | 1.5rem |
| вертикальные поля | 2rem | 1rem |
| зазор между блоками | 2rem | 1rem |
| длительности перехода | 0.25s и 0.125s | только 0.125s |
| движение каретки | 150 / 100 / 85 мс | 250 / 120 / 60 мс |
| размер шрифта слов | 32px | 24px |
| кнопка навигации | 36×36 | 28×28 |
| брейкпоинты | 7: 331, 426, 721, 849, 1105, 1361, 1617 | 1 |
| `::selection` | фон `main`, текст `sub-alt` | текст `bg` |
| `:active` | есть | нет |
| `:focus-visible` | у всех кнопок | у одной |
| `prefers-reduced-motion` | учитывается | нигде |
| `aria-label` у иконок | у всех | 3 на весь фронтенд |

---

## ОПАСНОСТЬ — метрики

Четыре счётчика символов (244) и raw по секундам на графике (251)
требуют правок в движке. Формулы у нас продублированы: клиент считает
для показа, сервер — для сохранения.

**Меняешь формулу — меняй одновременно в `frontend/src/core/metrics.ts`
и `backend/app/services/metrics.py`.** Иначе показанные цифры разойдутся
с сохранёнными, и обнаружится это не сразу.

Есть тесты, которые стерегут расчёты: в прошлой версии `consistency` был
случайным числом. **Не ослабляй их.** Мешает тест — неверна правка.

---

## ПОРЯДОК РАБОТЫ

1. **Перепроверка статуса** всех 270 пунктов по текущему коду. Сводка
   ведётся в `audit/main-page-status.md` — этот файл пишет сессия-координатор,
   возьми числа оттуда, если он уже готов, и не дублируй работу.
2. **Сломанное, что ещё осталось**: рестарт на экране результата (220),
   таймер `mini` (175), pace caret в режимах «средняя», «рекорд»,
   «последний» (165–167). Пункты `esc` (218) и подсветка навигации (26)
   уже закрыты — проверь и пропусти.
3. **Переименования под темы, оставшиеся**: `#caret.default` с миграцией
   сохранённого значения (148), `.blind` (119), `incorrect extra` (109),
   `typed-effect-*` (131), дефисы в подсветке (128), `keymap` (206).
   `leaderboards` (12), `logoText` (6) и `testConfig` (43) уже сделаны.
4. **Гашение при наборе** — сделано целиком (шапка, подпись, футер,
   подсказки). Проверь и переходи дальше.
5. **Огромный таймер** (174) и полоса на весь экран (173). **Это теперь
   самый крупный незакрытый пункт главной** — начинай с него.
6. **Панель режимов**: три карточки, отступы, длины цитаты (44–65).
7. **Числа и состояния**: шрифт 32px, каретка 150/100/85, поля 2rem,
   `:active`, `:focus-visible`, `prefers-reduced-motion`.
8. **Строка активных режимов** (76–94).
9. **Экран результата** без движка: тип теста, кнопки, источник цитаты,
   таймаут и показ до сохранения.
10. **Область слов**: wrapper вместо `#words`, обводка ошибки, следы
    исправлений, подсветка на два и три слова.
11. **График и четыре счётчика** — движок и метрики парой.
12. Семь брейкпоинтов.

---

## ЧЕГО НЕ ДЕЛАТЬ

- Не реализуй по отчёту, не проверив текущий код.
- Не переименовывай `#words`, `.word`, `<letter>`, `#caret` — на них
  держатся 52 файла тем.
- Не переименовывай стиль каретки без миграции сохранённого значения.
- Не трогай формулы метрик поодиночке — только парой.
- Не ослабляй тесты, стерегущие расчёты.
- Не ставь `done: true` настройке, логику которой не написал.
- Не тащи зависимости — ни библиотеку графиков, ни фреймворк.
- Не начинай, если файлы правит другая сессия.
- Heredoc в bash ломается об апострофы в русских текстах и CSS — файлы с
  кавычками пиши инструментом Write.

---

## ПРОВЕРКИ

```bash
cd backend && .venv/Scripts/python -m pytest -q
```

```bash
cd frontend && npm run typecheck && npm test && npx vite build
```

Руками, на главной:

- пройди тест до конца и посмотри результат глазами
- пройди тест **гостем**, без входа
- **оборви сеть** на сохранении: цифры должны появиться всё равно
- проверь `esc`, `tab`, `enter` — и до теста, и на экране результата
- включи Caps Lock **до** начала ввода
- все четыре стиля каретки и три скорости
- все режимы подсветки, лента, слепой режим
- **не меньше десяти тем**, включая светлую, и funbox `crt`, `space_balls`,
  `mirror`
- весь интерфейс **только с клавиатуры** — везде видимое кольцо фокуса
- системную настройку «уменьшить движение» — анимации должны погаснуть
- все семь брейкпоинтов

Начни с шага 1 и покажи мне статус до того, как что-либо менять.
