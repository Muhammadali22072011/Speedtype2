# План работ — проход C

## Что уже сделано (первый заход)

Уровень 0 закрыт целиком, часть уровня 1 и контракта тем — тоже.

| было | стало |
|---|---|
| `quickRestart: "esc"` не работал (сравнивалось `"escape"` с `"esc"`) | клавиша нормализуется, esc перезапускает тест |
| рестарт с клавиатуры умирал на экране результата | обработчик переехал с `#wordsInput` на `document`, проверено вживую |
| активный пункт меню не подсвечивался никогда (`.nav-link` против `[data-nav-item]`) | подсвечивается |
| шапка не обновлялась после входа | `onAuthChange` в `api/client.ts`, меню перерисовывается сразу |
| `timerStyle: "mini"` был неотличим от `text` | отдельная ветка и класс |
| `paceCaret` всегда брал своё значение | `average`, `pb`, `last` и новый `daily` берут данные с сервера, ответ кэшируется |
| тема писалась inline на `<html>` и блокировала `:root{}` из css | вставляется тегом `<style>` первым в `<head>` |
| funbox `crt` был мёртв (css ждёт `body.crtmode`) | класс на `body` + элемент `#scanline` |
| `no_quit` ничего не делал | тест нельзя бросить, пока он идёт |
| три строки ограничивались через `3lh`, нижняя обрезалась | высоту обёртки считает JS по реальной высоте слова |
| прокрутка съезжала: шаг строки считался без полей слова | общий `lineHeight()` для обёртки и прокрутки |
| подсказка «нажмите сюда» лежала на чётких словах | `applyLook` больше не стирает класс `blurred`, слова гаснут до 0.25 |
| esc уводил в настройки | esc открывает командную строку, как у них |
| подсказка клавиш врала при `quickRestart: off` и `esc` | текст строится от настройки |
| каретка мигала во время набора | мигание останавливается на 600 мс после нажатия |
| классы `blindMode`, `typed-*`, `highlight-next_word`, `caret-line` | `blind`, `typed-effect-*`, `highlight-next-word`, `#caret.default` — как у monkeytype |
| не было `next_two_words` и `next_three_words` | есть, вместе с css |
| лишняя буква имела только класс `extra` | `incorrect extra`, как у них |
| `colorfulMode` красил только рамку | переопределяет все четыре переменные, есть `flipped.colorfulMode` |
| подсветка работала прозрачностью | работает цветом, как у них |
| нет страницы «о проекте» и текста GPL-3.0 | есть `/about`, пункт меню `about` (его красят 16 тем) |
| нет `.page`/`.pageAbout` на контейнере | роутер ставит, funbox `space_balls` и темы находят |
| подсказки на `title=` | свои balloon-подсказки на `[aria-label][data-balloon-pos]` |
| `quickEnd`, `strictSpace`, `deleteOnError` — заглушки | работают в движке |
| `playTimeWarning` — заглушка | играет `timeWarning.wav`, файл лежал без дела |
| `randomTheme`, `autoSwitchTheme`, `themeLight`, `themeDark`, `favThemes` — заглушки | работают, в пикере тем появилась звезда избранного |
| `zipf` — пустышка | сервер отдаёт выборку, смещённую к началу частотного словаря |
| funbox-пустышки были неотличимы от рабочих | помечены `done: false`, в списке приглушены и подписаны |
| README врал про числа | приведён к фактам, числа считаются из кода |
| нет `:focus-visible`, `:active`, `prefers-reduced-motion` | есть |
| ширина, отступы, размеры кнопок и каретки на глаз | сверены с их `getComputedStyle` и приведены |
| `minWpm`, `minAcc`, `minBurst` — заглушки | тест обрывается по порогу скорости, точности и темпа слова; добавлен недостающий ключ `minBurstCustomSpeed` |
| `repeatQuotes` — заглушка | при рестарте отдаётся та же цитата |
| `indicateTypos` — заглушка | `below` подписывает набранное под буквой, `replace` подставляет вместо неё |
| `showAverage` — заглушка | средние скорость и точность в строке режимов |
| `singleListCommandLine` — заглушка | плоский список: каждое значение отдельной командой |
| нет строки активных режимов | есть: язык, funbox, сложность, слепой режим, темп, пороги, уверенность, стоп и стирание на ошибке, раскладка, «сохранение выключено» — 13 индикаторов, гаснут при наборе |
| язык и funbox жили в панели режимов | переехали в строку режимов, как у monkeytype |
| `paceCaret` без варианта `daily` | добавлен |
| шрифт задавался на `#words`, каретка считала em от body | размер живёт на `#wordsWrapper`, каретка вдвое шире и совпадает с их 3.2px |

| экран результата: два числа символов | четыре — верные, неверные, лишние, пропущенные; движок считает их отдельно, **не трогая формулы метрик** |
| нет типа теста на результате | «слова 10 english пунктуация» |
| одна кнопка «ещё раз» | следующий тест, повторить тот же текст, практика ошибочных слов, разбор слов, лидерборд |
| гостю показывался текст | ссылка «войдите, чтобы результат попал в лидерборд» |
| живые показатели одной строкой над словами | три места, как у них: крупный таймер над словами, крупные скорость и точность под, мелкая строка сверху, полоса времени приклеена к верху окна |

Заглушек в `config-spec.ts` было 24 из 84, стало **6 из 85**:
`quoteLength`, `britishEnglish`, `lazyMode`, `oppositeShiftMode`,
`codeUnindentOnBackspace`, `monkey`.

### Ещё две поломки того же рода, найденные по ходу

Обе — следствие того, что экран результата заменяет разметку теста целиком,
а ссылки на узлы остаются старыми:

- кнопки «повторить тот же текст» и «практика ошибочных слов» рисовали
  в отсоединённые узлы, и на экране не появлялось ничего. Теперь они
  пересоздают страницу и передают ей готовый текст;
- `applyLook()` пересобирал класс `#words` целиком и стирал `blurred`,
  из-за чего подсказка «нажмите сюда» оставалась лежать на чётких словах.

---


Всё, что в отчёте получило `НЕТ`, `ЗАГЛУШКА` или `ЧАСТИЧНО`, сведено в
очередь. Сортировка — по паре «заметность пользователю × стоимость»:
сверху то, что видно на первом экране и чинится дёшево, снизу то, что
дорого и видно редко.

Оценка строк — порядок величины, а не смета. «Что сломается» — реальные
риски, а не формальность.

Номера в скобках — строки [report.md](report.md).

---

## Уровень 0. Сломанное. Чинить первым, это не улучшения, а починка

| # | что | файлы | ~строк | что сломается |
|---|---|---|---|---|
| 0.1 | `quickRestart: "esc"` не работает: сравнивается `"escape"` с `"esc"` (218) | `pages/test.ts:684-696` | 6 | ничего; заодно поправить подсказку, которая сейчас пишет «esc заново … esc настройки» |
| 0.2 | Рестарт с клавиатуры мёртв на экране результата: `finishTest` затирает `#wordsInput` вместе с обработчиком (220) | `pages/test.ts:615-656, 717` | 20 | перенос `keydown` на `document` требует проверок «фокус не в поле ввода» — иначе рестарт будет срабатывать при наборе в модалках |
| 0.3 | Активный пункт навигации не подсвечивается: роутер ищет `.nav-link`, а в разметке `[data-nav-item]` (26) | `router.ts:38-40` | 3 | ничего |
| 0.4 | `timerStyle: "mini"` ведёт себя как `"text"` — ветки нет (175) | `pages/test.ts:408-421`, `styles/modes.css` | 25 | ничего |
| 0.5 | `paceCaret` игнорирует режим: `average`/`pb`/`last` берут `paceCaretCustomSpeed` (165–167) | `pages/test.ts:453-468`, `api/client.ts` | 40 | нужен запрос статистики до старта теста — добавится сетевой вызов на каждый рестарт, кэшировать |
| 0.6 | funbox `crt` мёртв: css ждёт `body.crtmode`, мы ставим класс на `#words` (525) | `core/funbox.ts:296-312`, `pages/test.ts:92-117` | 15 | нужен ещё элемент `#scanline`; проверить, что класс снимается при уходе со страницы |
| 0.7 | Тема inline-стилями на `<html>` блокирует `:root{}` из css тем и funbox (437, 524) | `state/themes.ts:32-54` | 15 | переход на `<style id="theme">`: следить за порядком — тег темы должен идти **до** `<link>` темы и funbox |
| 0.8 | 10 funbox-пустышек помечены как рабочие (`zipf`, `weakspot`, `poetry`, `wikipedia`, `polyglot`, `arrows`, `layout_mirror`, `memory`, `no_quit`, `layoutfluid`) | `core/funbox.ts` | см. 5.4 | пока не написаны — честно пометить в пикере, как это сделано для настроек |
| 0.9 | Шапка не обновляется после входа: `renderNav()` не вызывается, `pushState` не порождает `popstate` — остаются «войти» и «регистрация» (370) | `pages/auth.ts:60-61`, `main.ts:104-111` | 5 | ничего; заодно подписать `renderNav` на смену токена |
| 0.10 | README врёт про числа (541–544, 550–552) | `README.md` | 20 | ничего |

**Итого уровень 0: ~150 строк.** Половина — однострочные правки с большим
эффектом.

---

## Уровень 1. Первый экран, дёшево, видно сразу

| # | что | файлы | ~строк | что сломается |
|---|---|---|---|---|
| 1.1 | Каретка перестаёт мигать при наборе (158) | `pages/test.ts:342-379`, `styles/monkeytype.css:320-347` | 15 | ничего |
| 1.2 | Вторая анимация мигания `caretFlashHard` при `smoothCaret: off` (157) | `styles/monkeytype.css` | 10 | ничего |
| 1.3 | Числа движения каретки привести к их: 150/100/85 мс (159–162) | `styles/modes.css:83-86` | 4 | ничего |
| 1.4 | Скругление каретки `--roundness` вместо `0.1em` (156) | `styles/monkeytype.css:325` | 1 | ничего |
| 1.5 | Обводка `text-shadow` у слова с ошибкой (116) | `styles/monkeytype.css:296-299` | 8 | в светлых темах обводка цветом фона — проверить на `serika` |
| 1.6 | Размытие при потере фокуса + `opacity: .25` (140, 193) | `styles/monkeytype.css:315-318` | 2 | ничего |
| 1.7 | Дефолты в соответствие: `fontSize: 2`, `timerStyle: mini`, `liveSpeedStyle: off`, `smoothLineScroll: false`, `soundVolume: 0.5`, `caretStyle: default` | `state/settings.ts:113-205` | 8 | у существующих пользователей настройки в localStorage — на них не повлияет; новые увидят другой вид по умолчанию |
| 1.8 | Логотип: вес 400, letter-spacing normal, подпись 0.315em (6, 7) | `styles/monkeytype.css:111-123` | 5 | ничего |
| 1.9 | Кнопки навигации 36×36 с паддингом и скруглением (23, 24) | `styles/monkeytype.css:132-144` | 6 | ничего |
| 1.10 | Панель режимов: три карточки вместо одной, отступы кнопок `12px 6px` (44, 47–50) | `pages/test.ts:180-226`, `styles/monkeytype.css:155-209` | 40 | панель станет заметно выше — проверить вертикальный ритм страницы |
| 1.11 | Ширина контента и поля: 1024/2rem/2rem, `row-gap: 2rem` (442–445) | `styles/monkeytype.css:78-86` | 6 | всё сдвинется; проверить лидерборд и настройки |
| 1.12 | `::selection` цвет текста `sub-alt`, `::placeholder` — `sub` (456, 457) | `styles/monkeytype.css:71-74` | 5 | ничего |
| 1.13 | `:focus-visible` для всех кнопок и ссылок (465) | `styles/monkeytype.css` | 10 | ничего |
| 1.14 | Убрать `tabindex="1"` у `#wordsInput` — он ломает порядок обхода (471) | `pages/test.ts:49` | 1 | проверить, что фокус после загрузки всё ещё попадает в поле |
| 1.15 | `aria-label` на все иконочные кнопки (469) | `main.ts`, `pages/*.ts` | 20 | ничего |
| 1.16 | `prefers-reduced-motion` — отключить анимации каретки, прокрутки, funbox (470) | `styles/*.css` | 15 | ничего |

**Итого уровень 1: ~156 строк.**

---

## Уровень 2. Первый экран, дороже, но это и есть «вид monkeytype»

| # | что | файлы | ~строк | что сломается |
|---|---|---|---|---|
| 2.1 | Живая статистика по их схеме: огромный таймер над словами, живые показатели под словами, `mini` сверху слева, полоса `fixed` наверху экрана (173–183) | `pages/test.ts:401-449`, `styles/modes.css:99-118`, разметка `test.ts:39-57` | 120 | полная перестройка блока: сломается вёрстка `#typingTest`; таймер с `z-index: -1` требует, чтобы у `#words` не было своего фона |
| 2.2 | Строка активных режимов (`TestModesNotice`) — 20 индикаторов (76–94) | новый `ui/modes-notice.ts`, `pages/test.ts` | 180 | язык и funbox уезжают из панели режимов — переписать обработчик кликов панели |
| 2.3 | Приглушение шапки и футера во время набора: `header[data-focused]` (9, 30, 39, 492) | `main.ts`, `pages/test.ts`, `styles/monkeytype.css` | 25 | 7 тем начнут применять свои правила `header[data-focused]` — визуально изменится больше, чем кажется, это и есть цель |
| 2.4 | Ограничение трёх строк перенести на `#wordsWrapper` (99) | `pages/test.ts:319-340`, `styles/monkeytype.css:246-252` | 30 | прокрутка и позиция каретки считаются от `#words` — пересчитать |
| 2.5 | Экран результата: `test type`, символы 4 числа, ссылка «войдите, чтобы сохранить», размер крупных чисел (237, 240, 244, 266) | `pages/test.ts:615-656`, `core/engine.ts` | 70 | движок не разделяет `extra` и `missed` — придётся добавить два счётчика; **это трогает метрики**, значит правка одновременно в `core/metrics.ts` и `backend/app/services/metrics.py`, иначе цифры разойдутся |
| 2.6 | Кнопки результата: следующий тест, тот же текст, история слов (259, 260, 262) | `pages/test.ts` | 50 | «тот же текст» требует хранить сгенерированный список между тестами |
| 2.7 | График результата: raw пунктиром, burst, легенда-переключатели, подписи оси X (251–253, 256) | `pages/test.ts:789-826`, `core/engine.ts` | 120 | движок пишет только посекундный wpm — нужны ещё raw и ошибки по секундам; **снова обе стороны метрик** |
| 2.8 | Уведомления в углу вместо текста ошибки внутри страниц (433) | новый `ui/notifications.ts`, все страницы | 90 | места, где ошибка сейчас печатается в блок, надо переключить, иначе будет дублирование |
| 2.9 | Модалка подтверждения (`SimpleModal`) (425) | новый `ui/confirm.ts` | 50 | нужна для сброса настроек и удаления |
| 2.10 | Модалки своего времени и своего числа слов (407, 408) | `ui/modals.ts`, `pages/test.ts` | 60 | `timeValue`/`wordsValue` перестанут быть из фиксированного списка — проверить, что панель подсвечивает «своё» |
| 2.11 | Адаптив: 7 брейкпоинтов, панель режимов на мобильном модалкой (458–462, 73, 421) | `styles/*.css`, `ui/modals.ts` | 120 | сейчас `@media` всего два; на узком экране многое поедет |
| 2.12 | Анимация смены группы значений в панели (71, 450) | `pages/test.ts` | 40 | требует не перерисовывать панель целиком на каждый клик |

**Итого уровень 2: ~955 строк.**

---

## Уровень 3. Настройки

| # | что | файлы | ~строк | что сломается |
|---|---|---|---|---|
| 3.1 | Довести 24 заглушки. Разложены по цене ниже | `state/config-spec.ts`, `core/engine.ts`, `pages/test.ts` | см. таблицу | |
| 3.2 | Поиск по настройкам (274) | `pages/settings.ts` | 40 | ничего |
| 3.3 | Экспорт/импорт настроек JSON (298, 299, 399) | `pages/settings.ts`, `ui/commandline.ts` | 50 | импорт чужого JSON — валидировать по `config-spec` |
| 3.4 | Подтверждение сброса (300) | `pages/settings.ts` | 10 | зависит от 2.9 |
| 3.5 | Сворачивание разделов + подсказка про командную строку (275, 276) | `pages/settings.ts` | 30 | ничего |
| 3.6 | Превью темы и шрифта при наведении в командной строке (389, 390) | `ui/commandline.ts`, `state/themes.ts` | 40 | превью надо откатывать при закрытии без выбора |
| 3.7 | `esc` открывает командную строку вместо перехода в настройки (221, 378) | `pages/test.ts:692`, `ui/commandline.ts` | 10 | ломает привычку текущих пользователей — но это паритет |
| 3.8 | Ввод значения прямо в командной строке (394) | `ui/commandline.ts` | 40 | |
| 3.9 | Цветные плашки тем в командной строке (393) | `ui/commandline.ts` | 20 | |
| 3.10 | Недостающие значения перечислений: `highlight-next-two/three-words`, `deleteOnError: *_hard`, `indicateTypos: both`, `oppositeShiftMode: keymap`, `keymapStyle: split_matrix/steno/steno_matrix`, `paceCaret: daily`, `timerStyle: flash_*`, `randomTheme: custom/auto` (раздел «Расхождения в наборах значений») | `state/config-spec.ts` + логика каждого | 200 | добавлять значение без логики = новая заглушка; ставить `done: false` |
| 3.11 | Отсутствующие ключи: `paceCaretStyle`, `repeatedPace`, `minBurstCustomSpeed`, `compositionDisplay`, `customLayoutfluid`, `customPolyglot`, `customTheme`, `customThemeColors`, `monkeyPowerLevel`, `accountChart` | `state/config-spec.ts`, `state/settings.ts` | 150 | то же |

### 24 заглушки по цене

| настройка | цена | где делать |
|---|---|---|
| `quickEnd` | ~10 строк | `core/engine.ts:commitWord` |
| `strictSpace` | ~10 | `core/engine.ts:type` |
| `deleteOnError` | ~25 | `core/engine.ts` |
| `britishEnglish` | ~20 + данные | сервер: словарь замен из `constants/british-english.ts` |
| `lazyMode` | ~20 | сервер: снятие диакритики |
| `repeatQuotes` | ~15 | `pages/test.ts:startTest` |
| `quoteLength` | ~40 | сервер `/api/text` + клиент; сейчас длина цитаты не выбирается вовсе |
| `minWpm` + `minWpmCustomSpeed` | ~25 | `core/engine.ts`, обрыв теста |
| `minAcc` + `minAccCustom` | ~25 | там же |
| `minBurst` + `minBurstCustomSpeed` | ~30 | там же |
| `indicateTypos` | ~50 | разметка `.hints hint` в `pages/test.ts:renderWords` |
| `oppositeShiftMode` | ~60 | нужна карта «клавиша → рука» из раскладки |
| `codeUnindentOnBackspace` | ~15 | нужен режим кода, которого нет |
| `showAverage` | ~40 | нужна статистика по режиму с сервера |
| `themeLight` / `themeDark` / `autoSwitchTheme` | ~30 | `state/themes.ts` + `matchMedia` |
| `randomTheme` | ~30 | `state/themes.ts` |
| `favThemes` | ~40 | + кнопка в пикере и командной строке |
| `playTimeWarning` | ~15 | `core/sound.ts`, файл `timeWarning.wav` уже лежит в `static/sounds` |
| `singleListCommandLine` | ~30 | `ui/commandline.ts` |
| `monkey` | ~60 + картинки | новый `ui/monkey.ts`, 4 png |

---

## Уровень 4. Аккаунт

| # | что | файлы | ~строк | что сломается |
|---|---|---|---|---|
| 4.1 | Таблица личных рекордов по режимам (319) | сервер: новый эндпоинт + запрос; клиент: `pages/profile.ts` | 150 | считать рекорды на лету по `Result` можно без новой таблицы, но при росте истории станет медленно |
| 4.2 | Фильтры результатов (324) | `pages/profile.ts`, `api/routes/results.py` | 120 | параметры фильтра надо валидировать на сервере |
| 4.3 | Пагинация истории (325) | `pages/profile.ts` | 30 | сервер уже умеет `offset` — клиент просто им не пользуется |
| 4.4 | График скорости по времени + гистограмма (320, 321) | `pages/profile.ts` | 150 | рисовать так же вручную в svg, как график результата |
| 4.5 | Тепловая карта активности по дням (323) | `pages/profile.ts`, новый эндпоинт | 120 | нужна агрегация по дням на сервере |
| 4.6 | Тестов начато / оценка набранных слов (312, 315) | сервер + клиент | 60 | «начато» сервер сейчас не знает — клиент должен слать событие старта |
| 4.7 | Теги (297, 250, 327, 416, 415) | новая таблица, эндпоинты, модалки, командная строка | 300 | связь «результат — тег» многие-ко-многим, миграция БД |
| 4.8 | Пресеты (296, 417, 418) | новая таблица, эндпоинты, модалки | 250 | пресет хранит срез настроек — привязать к версии схемы, иначе старые пресеты сломаются |
| 4.9 | Страница настроек аккаунта: имя, почта, пароль, удаление, выгрузка, сброс статистики (329–335) | новая страница + 6 эндпоинтов | 350 | удаление аккаунта и смена пароля — трогают безопасность, нужны проверки текущего пароля |
| 4.10 | Аватар (306) | загрузка файлов на сервере | 120 | поле в API уже есть и не используется; нужна валидация размера и типа |
| 4.11 | Уровень и опыт, серия дней (310, 311, 18) | новые поля в `User`, начисление в `POST /results` | 200 | миграция БД; формула xp своя, копировать их не обязательно |
| 4.12 | Публичный профиль `/profile/:name` (336) | новая страница + эндпоинт | 150 | приватность: решить, что показывать чужим |

**Итого уровень 4: ~2000 строк.** Это самая большая яма и самая
дорогая. Разумно резать: 4.1 + 4.3 + 4.2 дают 80% ощущения «страница
аккаунта работает» за 300 строк.

---

## Уровень 5. Остальное

| # | что | файлы | ~строк | что сломается |
|---|---|---|---|---|
| 5.1 | Страница «о проекте» с текстом GPL-3.0 (372–377) | новая `pages/about.ts`, пункт навигации | 120 | обязательна при публикации; заодно закрывает `[data-nav-item="about"]`, который красят 16 тем |
| 5.2 | Лидерборд: вкладки «время 15 / 60», выбор языка, своя строка, пагинация (338, 339, 344, 352–354) | `pages/leaderboard.ts` | 120 | сервер уже принимает `mode`, `mode_value`, `language` — работа почти вся клиентская |
| 5.3 | Режим `zen` (59) | `config-spec.ts`, `core/engine.ts`, `pages/test.ts` | 120 | движок завязан на список слов; zen — набор без текста, отдельная ветка везде |
| 5.4 | Режим `custom` + свой текст (60, 403–406) | + модалки, `localStorage` | 200 | переносы строк и табы в тексте требуют `.newline` и `<textarea>` вместо `<input>` (215, 143) |
| 5.5 | 10 funbox-пустышек (0.8) | `core/funbox.ts`, сервер | 250 | `poetry`, `wikipedia` требуют внешних источников — либо свои данные, либо честно убрать из списка |
| 5.6 | 5 funbox, которых нет: `rAnDoMcAsE`, `sPoNgEcAsE`, `IPv4`, `IPv6`, `ALL_CAPS` | `core/funbox.ts` | 40 | ничего, они простые |
| 5.7 | Реплей теста (263) | `core/engine.ts` (запись таймингов) + `pages/test.ts` | 150 | движок начнёт хранить историю нажатий — следить за памятью на длинных тестах |
| 5.8 | Скриншот результата (264) | `pages/test.ts` | 80 | без внешних библиотек — только через `canvas`, вручную это дорого |
| 5.9 | Практика ошибочных слов (261) | `pages/test.ts` | 60 | |
| 5.10 | Цитаты: поиск, добавление, жалоба, оценка (409–412, 249) | сервер + 4 модалки | 250 | нужна модерация, иначе смысла нет |
| 5.11 | Своя тема и свой фон файлом (291, 294, 295) | `state/themes.ts`, `pages/settings.ts` | 150 | |
| 5.12 | Гонки: применять настройки отображения и звук (536–538) | `pages/race.ts` | 60 | сейчас у гонки своя урезанная отрисовка — либо вынести общий рендер, либо оставить как есть |
| 5.13 | Синхронизация настроек с сервером (303) | сервер + `state/settings.ts` | 120 | конфликт «локально изменил / на сервере другое» — нужна стратегия слияния |

**Итого уровень 5: ~1720 строк.**

---

## Отдельно: контракт для 52 файлов тем

Это не «фича», а причина, по которой темы monkeytype у нас работают не
целиком. Дешёвая часть — переименования, дорогая — недостающие элементы.

| # | что | файлы | ~строк | что сломается |
|---|---|---|---|---|
| Т.1 | `#caret.default` вместо `caret-line` и т. п. (505, 148) | `pages/test.ts:107`, `styles/modes.css:65-81` | 10 | значение `line` уже лежит у пользователей в localStorage — нужна миграция значения |
| Т.2 | `#words.blind` вместо `blindMode` (500, 119) | `pages/test.ts:95`, `styles/*.css` | 6 | |
| Т.3 | `typed-effect-*` вместо `typed-*` (131) | `pages/test.ts:101`, `styles/modes.css:15-30` | 6 | |
| Т.4 | `highlight-next-word` через дефис (128) | `pages/test.ts:100`, `styles/modes.css:38-40` | 4 | значение настройки остаётся `next_word`, меняется только класс |
| Т.5 | Буквы `extra` получают оба класса `incorrect extra` (109, 502) | `pages/test.ts:287` | 3 | правило `.extra` в css надо оставить |
| Т.6 | `[data-nav-item="leaderboards"]` (478, 12) | `main.ts:50` | 2 | ссылка остаётся `/leaderboard` |
| Т.7 | `[data-ui-element="logoText"]` на `h1` (486) | `main.ts:23` | 1 | |
| Т.8 | `[data-ui-element="testConfig"]` (491, 43) | `pages/test.ts:40`, `styles/*.css` | 4 | id оставить, добавить атрибут |
| Т.9 | `header[data-focused]` (492) | см. 2.3 | — | |
| Т.10 | Класс `.textButton` для текстовых кнопок (513) | `styles/*.css`, все страницы | 30 | 3 темы начнут красить кнопки — это и нужно |
| Т.11 | `<kbd>` вместо `.keyTip` (518) | `pages/test.ts:131-134`, `ui/commandline.ts` | 8 | |
| Т.12 | Классы страниц `.pageSettings`, `.pageAccount`, `.pageAbout` (515–517) | `router.ts`, страницы | 15 | |
| Т.13 | Подсказки на `[aria-label][data-balloon-pos]` (519, 27) | новый css + разметка | 60 | |
| Т.14 | `body.crtmode` и `#scanline` (509, 510) | см. 0.6 | — | |
| Т.15 | Шрифт `GallaudetRegular.woff2` для funbox `asl` (526) | `backend/app/static/webfonts/` | — | без него `asl` показывает обычные буквы |
| Т.16 | Класс `.page` на корне страницы для funbox `space_balls` (524) | `router.ts` | 3 | |

**Итого контракт тем: ~152 строки.** Соотношение цены и эффекта здесь
лучшее во всём списке: 152 строки правок «оживляют» половину из 52 файлов
тем и два funbox.

---

## Что предлагаю не делать

- **Вход через Google и GitHub** (365, 366) — тянет Firebase или свой
  OAuth-слой ради удобства входа. У нас свой JWT, этого достаточно.
- **Реклама** (`ads`) — намеренно оставлено единственное значение `off`.
- **Поддержка, discord, twitter в футере** (32, 34) — чужие ссылки.
- **Инбокс, PSA, друзья** (434, страница `/friends`) — социальная часть,
  к тренажёру отношения не имеет.
- **Cookie-баннер** (428) — мы ничего не собираем.
- **`monkeyPowerLevel`, `accountChart`** — скрытые настройки, их даже в их
  интерфейсе нет.
- **Weekly XP-лидерборд** (342) — другая метрика, требует системы опыта.

---

## Порядок, который я предлагаю

1. **Уровень 0** целиком (~145 строк). Это починка, а не улучшение.
2. **Контракт тем Т.1–Т.8, Т.11** (~44 строки) — переименования, дают
   несоразмерный эффект на 52 темах.
3. **Уровень 1** целиком (~156 строк) — числа, отступы, дефолты.
4. **2.1 + 2.3 + 2.2** (~325 строк) — после них главная перестаёт
   отличаться на глаз.
5. Дальше по списку.

Первые три пункта — примерно 345 строк и вся видимая разница на первом
экране. Остальное — работа на недели, и её объём стоит обсудить, прежде
чем начинать.
