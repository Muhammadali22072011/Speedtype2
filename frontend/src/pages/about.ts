/**
 * О проекте. Здесь же живёт обязательный текст лицензии: проект использует
 * код и данные monkeytype под GPL-3.0, и при публикации уведомление должно
 * быть видно в интерфейсе, а не только в файле репозитория.
 *
 * Разметка повторяет monkeytype (.pageAbout .section .title) — на этих
 * селекторах держатся их файлы тем.
 */

import type { PageContext } from "../router";
import { SETTINGS } from "../state/config-spec";
import { FUNBOXES } from "../core/funbox";
import { icon } from "../ui/icons";

const CHARS_PER_WORD = 5;

export function aboutPage({ container }: PageContext): void {
  const done = SETTINGS.filter((s) => s.done).length;

  container.innerHTML = `
    <div class="section">
      <h1 class="title">${icon("info")} о проекте</h1>
      <p>
        speedtype — тренажёр слепой печати. Тест на время, на количество слов
        или по цитате. Настроек — ${SETTINGS.length}, режимов funbox —
        ${FUNBOXES.length}, тем — 187, языков — 432, раскладок клавиатуры — 239.
      </p>
      <p>
        Работает без регистрации: результат сохранится как гостевой.
        Аккаунт нужен только чтобы попасть в лидерборд и видеть свою историю.
      </p>
    </div>

    <div class="section">
      <h2 class="title">${icon("chart")} как считаются показатели</h2>
      <p>
        Слово считается за ${CHARS_PER_WORD} символов — независимо от языка,
        как в monkeytype. Клиент отправляет только сырые счётчики, все итоговые
        числа считает сервер: результат, присланный с чужого клиента, доверия
        не заслуживает.
      </p>
      <ul class="aboutList">
        <li><b>wpm</b> — верно набранные символы, делённые на пять, за минуту.</li>
        <li><b>raw</b> — то же самое, но по всем нажатиям, ошибки не вычитаются.</li>
        <li><b>точность</b> — доля верных нажатий от всех нажатий. Исправленная
            ошибка остаётся ошибкой: считаем по ходу набора, а не по итоговому тексту.</li>
        <li><b>ровность</b> — насколько ровно шла скорость. Берём посекундные
            замеры wpm, считаем коэффициент вариации и переводим в проценты.</li>
      </ul>
    </div>

    <div class="section">
      <h2 class="title">${icon("keyboard")} горячие клавиши</h2>
      <p>
        <kbd>tab</kbd> — начать заново (клавишу можно поменять в настройках),
        <kbd>esc</kbd> — командная строка,
        <kbd>ctrl</kbd>+<kbd>shift</kbd>+<kbd>p</kbd> — она же,
        <kbd>ctrl</kbd>+<kbd>backspace</kbd> — стереть слово целиком.
      </p>
    </div>

    <div class="section">
      <h2 class="title">${icon("bolt")} что уже работает</h2>
      <p>
        Из ${SETTINGS.length} настроек полностью работают ${done}. Остальные
        показаны на странице настроек приглушёнными с подписью «пока не
        работает» — так честнее, чем прятать их и делать вид, что их нет.
      </p>
    </div>

    <div class="section">
      <h2 class="title">${icon("crown")} благодарности и лицензия</h2>
      <p>
        Темы, словари, цитаты, раскладки, звуки нажатий и имена css-классов
        взяты из <a href="https://github.com/monkeytypegame/monkeytype"
        target="_blank" rel="noopener">monkeytype</a> — проекта Montydrei
        и сотен его участников.
      </p>
      <p>
        monkeytype распространяется под <b>GNU General Public License v3.0</b>.
        Раз в этом проекте есть их код и данные, он тоже распространяется
        на условиях GPL-3.0: исходники открыты, уведомление об авторстве
        сохранено, и любая производная работа обязана оставаться свободной.
      </p>
      <p>
        Иконки — Font Awesome Free под CC BY 4.0.
        Полный перечень заимствований — в файле <code>LICENSE-NOTICE.md</code>
        репозитория.
      </p>
      <p class="sub">
        Эта программа распространяется в надежде, что она будет полезной,
        но БЕЗ КАКИХ-ЛИБО ГАРАНТИЙ; даже без подразумеваемой гарантии
        ТОВАРНОГО СОСТОЯНИЯ или ПРИГОДНОСТИ ДЛЯ ОПРЕДЕЛЁННОЙ ЦЕЛИ.
        См. текст GNU General Public License.
      </p>
    </div>
  `;
}
